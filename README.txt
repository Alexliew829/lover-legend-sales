Lover Legend Sales V14.5

V14.5 Live 特别佣金删除持久化修正：
- 删除特别佣金日期后，即使规则数组已经在本机变成空数组，按“储存直播佣金制度”仍会强制写入云端。
- 空数组 [] 会覆盖 Google Sheet 当前佣金设置及对应月份 month_commission 快照，旧特别佣金不会再次恢复。
- 删除后 Live 佣金立即恢复按主播一般佣金计算，避免继续用已删除的特别佣金计算工资。
- 不增加额外云端读取请求，不改变现有 Sales / Fair / Live 销售同步架构。
- 所有前端、Apps Script、Manifest、Service Worker Cache、Backup 与资源版本统一为 V14.5。

Lover Legend Sales V14.5

V14.5 Fair 跨设备同步修正：
- 云端同步前若 Fair 没有尚未储存的本机修改，同步完成后强制以最新 rows 重建 Fair 每日营业额输入框。
- 修复其他设备新增/修改 Fair 后，Home 与月销售记录已更新，但上方对应日期仍显示旧值的问题。
- 若同步前确实存在尚未储存的 Fair 输入，则继续保护本机输入，不被云端刷新覆盖。
- 不新增 Google Apps Script 请求，不改变 Revision、储存、佣金或数据结构。
- 所有版本号与缓存资源统一更新为 V14.5。

Lover Legend Sales V14.5

V14.5 Fair 云端同步显示修正：
- 其他设备更新 Fair 后，本机同步到新 rows 时会刷新 Fair 上方每日营业额输入框。
- 从 Home / 其他页面进入 Fair 时，会用目前 Memory 最新资料重新带入日期金额。
- 手动下拉 Refresh 后会同步刷新 Fair 日期输入框。
- 若 Fair 输入框存在尚未储存的修改，不会被后台自动同步覆盖。
- 不增加新的 Google Apps Script 请求，只做本机 UI 重新渲染，不影响同步速度。
- 所有版本号、资源参数、Manifest、Service Worker Cache、Apps Script 与 Backup 统一为 V14.5。

Lover Legend Sales V14.5

V14.5 启动同步修正：
- 打开网页或手机 App 时，已有本机资料只执行轻量 Revision 检查。
- Revision 相同立即显示已同步，不读取整个月 Google Sheet。
- Revision 不同才下载当前月份资料。
- Revision 检查超时或失败时保留本机资料，显示“云端暂未确认”，不再自动进入慢速完整同步。
- 没有本机资料时仍会安全读取当前月份，确保首次安装不会缺资料。
- 手动下拉 Refresh 仍会强制读取最新云端资料。
- 所有版本号与缓存统一更新为 V14.5。

Lover Legend Sales V14.5

V14.5 Fair 储存与同步修复：
- Fair Save 重构为一次 Google Sheet 写入、一次本机 Memory 批量更新、一次完整 Render。
- Fair 储存后立即刷新当日销售、本月记录、本月总额、Home Fair、Home Total 与 Report。
- 合并被重复定义的 renderAll，避免后方旧函数覆盖 Fair 刷新逻辑。
- 同月份 loadMonth 使用共用 Promise，请求进行中时复用，减少 Dashboard、Report 与月份切换的重复读取。
- 保留本机 Mutation 时间保护，旧云端回应不会覆盖较新的 Fair 修改。
- 所有版本号、资源参数、Manifest、Service Worker Cache 与 Backup 版本更新为 V14.5。

Lover Legend Sales V14.5

本次更新：
- 保存营业资料只清除目标月份缓存，不再连带清除全年及全局缓存。
- Settings 使用独立的 10 分钟缓存，减少重复读取 Google Sheet。
- 相同页面同步继续共用单一请求；每月汇总仅在用户点击时读取全年资料。
- Fair／Live 佣金无变化时不重复写入云端。
- More 页面若 Fair 佣金机制或直播佣金制度尚未储存，会阻止切换页面并提示先储存。
- 关闭或刷新网页时，如佣金尚未储存，会显示浏览器离开确认。
- 所有版本号及 Service Worker Cache 已更新为 V14.5。

Lover Legend Sales V14.5

V14.5 Live 佣金同步修正：
- 删除特别直播佣金后，本机状态立即生效，旧云端回应不能再把已删除规则恢复。
- Live 佣金设置加入递增 Revision，较旧请求或较旧装置回应会自动忽略。
- Google Apps Script 快速写入同时保存停用主播、主播佣金及特别佣金日期。
- Google Apps Script 超时不会回滚本机资料；系统会保留修改并自动后台重试。
- 缩短快速佣金同步等待时间，不扫描或重写营业记录。
- Home、Live 与 More 继续即时使用本机最新佣金设置。
- 所有前端、Apps Script、Manifest、Service Worker、缓存和 Backup 版本统一为 V14.5。

Lover Legend Sales V14.5

V14.5 修正：
- More 的 Live 特别佣金月份独立使用系统当前月份，不再受 Home 历史月份选择影响。
- 修改特别佣金输入后直接按储存，会先提交当前编辑内容，再同步云端。
- Live 佣金按每笔记录实际日期读取对应月份规则；Home 与 Live 保存后立即重算。
- 保持快速设置写入，不扫描或重写营业记录。

Lover Legend Sales V14.5

V14.5 更新：
- “删除主播”改为“离职／停用”，执行前必须确认。
- 停用后只从 More 与 Live 当前主播名单隐藏，并清除当前一般佣金率；所有历史 Live、Home、Report 与佣金记录保持不变。
- 停用主播再次在 Live 输入同一名字并保存时自动复职，恢复原显示名字并对应原有历史资料，一般佣金默认 10%。
- 主播身份比较忽略英文字母大小写与全部空格（包括中间空格），但保留符号；Alex、alex、A lex 视为同一人，Alex+ 与 A+lex 视为不同人。
- 停用／复职只写入轻量佣金设置，不重新同步整批营业资料，避免影响原有同步速度。

Lover Legend Sales System V14.5

V14.5 本次修正：
- 新增主播后，各主播一般佣金默认 10%，不再以 0% 计算。
- 新增或删除直播特别佣金日期后，开始日期、结束日期和佣金率输入框全部清空。
- 修改主播佣金率后，Live 页面、Home、Report 和佣金合计立即同步重算，不需要 Refresh。
- 储存直播佣金制度改为单次批量写入 Google Sheet，减少云端写入次数。
- 直播特别佣金日期可直接点击日期文字框或日历按钮打开日期选择器。
- Frontend、Apps Script、Manifest、Service Worker、Version 与资源版本统一更新为 V14.5。

- Live 页面下方销售记录改为跟随 Live 所选日期的月份；选择 29-07-2026 即显示 07-2026 的 Live 记录与总额。

- 修复每次打开系统后，Home 主播佣金暂时显示 0% 的问题。
- 启动时先使用本机缓存的主播独立佣金，再由 Google Sheet 后台确认更新。
- 已结算月份若旧佣金快照缺少主播佣金，会自动使用最新已保存的主播独立佣金，不再变成 0%。
- 修改并储存主播佣金后，Home 与 Report 会立即重新计算并更新，无需 Refresh。
- 云端保存成功后再次确认 Home、Report 和本机缓存。
- Backup 文件名、Service Worker Cache、Frontend、Apps Script、Manifest、Version 等全部更新为 V14.5。

- 恢复 Fair 默认 6% / 7% / 8% 后，Home 立即重算并同步 Google Sheet。

- V14.5：每次新开网页在解锁后强制读取一次云端；同一页面只允许一项启动同步，并取消启动完成5秒后的重复检查。

- V14.5：密码画面使用关键样式并提前初始化；大型 Logo、业务脚本、Service Worker 与云端同步全部延后到解锁后。手机完全关闭再打开仍会强制检查 Google Sheet。

- V14.5：修复上一版 access.js 语法错误；业务脚本改为并行下载、顺序执行；静态资源改用快速缓存；压缩首页 Logo；所有版本号统一为 V14.5。

V14.5 启动同步优化：
- 密码画面显示期间后台预热 Google Apps Script，不阻塞登录。
- 首次读取优先读取当月工作表；当月表存在资料时不再扫描整张旧年份工作表。
- 启动时先完成云端月份读取，再重试未同步资料，避免两个请求互相拖慢。
- 静态资源使用 Cache First，首页与 version.json 保持 Network First。
- 手机或电脑完全关闭后重新打开，仍会执行一次新的云端月份检查。

V14.5 修正：
- 登录框关键样式与完整 style.css 完全一致，避免首次显示后突然变大。
- 为 Face ID / Passkey 按钮预留固定空间，按钮出现时不再推动登录框。
- Face ID 在页面稳定绘制后启动，减少手机登录画面跳动。
- 保留每次新开网页强制读取 Google Sheet。
- 手机关闭、切换后台再打开或从页面缓存恢复时，仍会重新检查云端资料。
- Service Worker Cache、前端资源、Backup、Manifest、Version 与 Apps Script 版本统一为 V14.5。

V14.5 电脑 Refresh 修正：
- 修复桌面浏览器保留已登入 Session 时，access.js 早于业务脚本载入器发出解锁事件，导致 Refresh 后业务脚本没有启动并卡在「正在准备资料」。
- 解锁状态改为可重放，业务脚本载入器无论先后顺序都会且只会启动一次。
- 首次同步完成前，focus / visibilitychange 不再启动第二轮同步。
- Service Worker 更新检查改为首次同步完成后再执行，不与冷启动抢资源。
- 手机完全关闭后重新打开仍会强制读取 Google Sheet。

V14.5 新增：
- Home 月份区新增查看日期，默认今天。
- 红绿状态按所选日期显示 Balakong / Belimbing 是否有记录；有记录时直接显示该日营业额。
- 手机 Balakong 与 Belimbing 卡片新增“本日”金额，与本月金额并排显示。
- 所有本日金额均直接使用已下载到本机的当月 rows 计算，同月份切换日期不会新增云端请求，不影响同步速度。
- 选择其他月份的日期时，才会沿用原有历史月份读取机制。
- 保留手机完全关闭后重新打开强制读取 Google Sheet。

V14.5 修正：
- 每月汇总按钮点击时按年度读取完整云端资料，恢复6月、7月等历史月份，不影响首次启动同步。
- 取消手机版 Balakong / Belimbing 卡片右侧重复的“本日”金额；所选日期营业额继续显示在上方红绿状态框。
- 本月营业额与本年度累计各自增加清楚的整体边框，方便区分统计范围。
- 保留手机完全关闭后重新打开强制读取 Google Sheet。

V14.5 修正：
- Home 的“查看日期”移动到“月份”上方，显示顺序改为日、月、年。
- 每月汇总点击后立即显示，不再出现“正在读取全年月份资料”。
- 当前月份同步完成后，系统在后台静默预载整年资料；不阻塞登录、Home 或首次同步。
- 6月、7月等历史月份会保留在本机年度资料中，打开每月汇总时直接显示。
- 手机完全关闭后重新打开仍会重新读取 Google Sheet。

V14.5 UI 统一：
- Home 查看日期统一使用 dd-mm-yyyy 显示，并保留日历选择。
- 所选日期状态简化为 Balakong / Belimbing + RM 金额，不重复显示日期。
- 没有记录时明确显示 RM0.00 · 没有记录。
- 状态金额加粗，提高手机阅读性。
- 本月与本年度区块继续使用独立边框，年度标题强化区分。
- 不增加任何新的同步请求，所有本日金额仍由本机当月资料计算。
- 手机完全关闭后重新打开仍会重新读取 Google Sheet。

V14.5 新增所选日期营业汇总：
- Balakong：所选日期营业额。
- Belimbing：所选日期营业额。
- Fair：所选日期所有 Fair 地点营业额总和。
- Live：所选日期所有主播营业额总和。
- Total：以上四项所选日期营业额总和，并以较大字体显示。
- 所有金额直接使用已同步到本机的 rows 计算，不增加云端请求，不影响同步速度。
- 手机完全关闭后重新打开仍会重新读取 Google Sheet。

V14.5：
- Balakong 本月与 Belimbing 本月改为同一行左右并排。
- 两张卡片宽度各占一半，高度统一。
- 手机横向间距缩小，金额保持大字并自动适应宽度。
- Fair、Live 与本月总营业额继续保持全宽。
- 不增加任何同步请求，不影响同步速度。
- 手机完全关闭后重新打开，仍会重新读取 Google Sheet。

V14.5：
- 本日营业额新增独立红色边框区块。
- 原本本月区块内的当日状态与 Total 已移到本日营业额区块。
- 本月营业额整体往下移动，并保留 Balakong / Belimbing 同行显示。
- 本年度累计继续使用独立金色边框。
- Home 现在清楚分为：本日营业额 / 本月营业额 / 本年度累计。
- 不增加任何同步请求，不影响同步速度。
- 手机完全关闭后重新打开仍会重新读取 Google Sheet。

V14.5 佣金保存优化：
- Fair 与 Live 佣金使用独立的快速云端保存接口。
- Settings 改为单一 JSON 设置行写入，不再每次清空并重写整张 Settings。
- Fair 保存只更新 Fair 比率；Live 保存只更新主播名称与主播比率。
- 已结算月份的佣金快照继续同步更新，不改变云端计算逻辑。
- 保存请求超时缩短并仅自动重试一次，不触发 Home / 全年资料重新同步。
- 第一次进入系统和原有云端同步流程完全不变。
- 手机完全关闭后重新打开仍会重新读取 Google Sheet。

V14.5 Live 日期佣金制度：
- 新日期区间佣金只套用 Live，Fair 继续使用整月营业额 6% / 7% / 8% 旧算法。
- 可新增 Live 特别佣金开始日期、结束日期及佣金率。
- 日期区间不能重叠；每笔 Live 销售按其销售日期自动寻找对应特别佣金。
- 没有特别日期规则时，继续使用各主播的一般佣金。
- Live 月佣金、Report 及 Excel 导出均按每笔销售日期分别计算后加总。
- 新增未来日期规则不会改变其他日期的佣金。
- 所有规则同步保存到 Google Sheet、Backup 与 Restore。
- 第一次进入系统、现有同步速度及手机关闭后重新读取云端机制不变。

V14.5：
- Live 页面每笔销售新增佣金率与佣金金额。
- 每位主播显示销售总数及佣金总数，页面底部显示 Live 本月总销售额及总佣金。
- 特别佣金日期统一使用 dd-mm-yyyy。
- 新增规则时结束日期可留空，代表活动正在进行。
- 活动结束后再填写结束日期；结束日期隔天自动恢复主播一般佣金。
- 第二场活动需新增另一条特别佣金日期，系统禁止日期重叠。
- 按钮改为“新增特别佣金日期”。
- 不增加初始同步请求，不改变首次进入系统及云端同步流程。
- Fair 维持原本整月 6% / 7% / 8% 计算。

V14.5 Live 月份归档逻辑：
- Live 特别佣金规则按月份独立管理。
- 系统进入新月份后，特别佣金列表自动显示为空；主播一般佣金继续保留。
- 未填写结束日期的特别佣金，只生效至该月最后一天；下个月自动恢复主播一般佣金。
- 特别佣金不能跨月份。下个月有新活动时，新增另一条规则。
- 旧月份的不同日期佣金率继续保存在 Google Sheet、月份佣金快照、Backup 与 Restore 中。
- 查看历史月份时，Live 销售记录、佣金率、佣金金额及总数仍按该月历史规则计算。
- 不增加任何首次进入或初始同步请求，不改变现有同步速度。

V14.5：
- Sales 页面选择任意日期后，下方显示该日期所属月份的全部 Sales 销售记录。
- Sales 记录按 Balakong / Belimbing 分组，并显示各公司总数及该月总销售额。
- Fair 页面选择开始日期或结束日期后，下方显示该日期所属月份的全部 Fair 销售记录。
- Fair 记录按地点分组，并显示各地点总数及该月总销售额。
- 例如 Fair 选择 18-08-2026，会显示 08-2026 全部 Fair 地点和销售记录。
- 同月份切换日期只用本机资料，不新增云端请求；只有本机缺少该历史月份时才读取该月份。
- Live 页面继续维持相同月份查看逻辑。
- 不增加首次进入系统的同步请求，不影响初始速度。

V14.5：
- Fair 无地点时只保留查看日期与当月销售记录。
- Fair 有地点时恢复开始/结束日期、每日营业额与储存。
- More 直播特别佣金日期单击即可打开日历。
- 不增加同步请求。

V14.5：
- Fair 无地点时显示所选日期全部地点销售与当日总销售，然后显示整月记录。
- Live 无主播时显示所选日期全部主播销售与当日总销售，然后显示整月记录。
- 输入 Fair 地点或选择 Live 主播后隐藏跨地点/跨主播当日汇总。
- 直播特别佣金日期改用与 Live 页面相同的日期选择绑定。
- 不增加云端请求，不影响首次进入及同步速度。

V14.5：
- 删除 Live 特别佣金日期规则后，自动清空开始日期。
- 自动清空结束日期。
- 自动清空特别佣金百分比输入框。
- 不影响各主播一般佣金。
- 不改变云端同步、首次进入速度或其他计算逻辑。

V14.5：
- 新增特别佣金规则后，开始日期、结束日期和佣金率自动清空。
- 点击已新增规则的日期和佣金率，可载入并修改开始日期、结束日期及佣金率。
- 修改时继续使用日历选择日期，不需要手动输入。
- 更新时忽略正在修改的规则本身，但禁止与其他规则重叠。
- 新增 Live 主播后，各主播一般佣金默认 10%，不再默认 0%。
- 储存直播佣金制度改成本机先储存、云端后台同步，减少按钮卡住和超时。
- 云端失败时保留本机资料，并显示“本机已储存，云端稍后重试”。
- 缩短云端锁等待和请求超时，只自动重试一次。

V14.5 同步修正版：
- 保存直播佣金后，Home、Live 与 Report 切换页面时立即重新计算，不需要刷新。
- 新增或删除特别佣金规则后，只清空上方输入框；下方未储存规则会保留。
- 云端后台读取不会覆盖尚未按“储存直播佣金制度”的本机修改。

V14.5 修复：
- Fair 地点比对忽略所有空格和英文字母大小写，同时保留符号。
- Fair 储存使用直接覆盖，不进行金额累加。
- Google Sheet 同日期、同地点的空格/大小写重复记录会在储存时自动合并，只保留最新一行。
- 修复 Fair 显示 RM1,378.00，但 Sheet 与 Home 错误显示 RM1,398.00 的重复记录问题。
